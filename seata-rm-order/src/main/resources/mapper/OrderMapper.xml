<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.nj.seatarmorder.mapper.OrderMapper">


    <insert id="create" parameterType="cn.nj.seatarmorder.entity.Order">
        INSERT INTO `order` (order_id, member_id, goods_id, points, quantity)
        VALUES (#{id}, #{memberId}, #{goodsId}, #{points}, #{quantity})
    </insert>
</mapper>
